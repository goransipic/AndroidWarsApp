<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="hr.goodapp.warsapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- TODO API IS USING HTTP FOR GETTING RESULTS example : ... "homeworld":"http://swapi.dev/api/planets/1/" .... Need to enable HTTP with usesCleartextTraffic NOT GOOD BUT FOR TESTING IS OK -->
    <application
        android:name=".WarsApplication"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WarsApp.DayNight">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>